#pragma once

#include <cassert>
#include<iostream>

template <class T>
class Array{

public:
    Array() {
        T array[] = 100,000;
    }
    T searchArray(T& item){
        for(int i 0; i<100,000){
            if (!Array[i]=UPC){
                i++;
            }
            else if (Array[i]=UPC{
                return UPC;
            }

        } 

    }

    void insertArray(T& item){
        array{i}=array[item];
    }

};